# playing around for now, may make more definite
run: "run-bd11mcal-t05"
sim: "sim-bd11"

fitter: "metacal-em"

# so we know how many pars
fit_model: "gauss"

noise: 0.001

em_pars:
  ngauss: 1
  ntry: 10
  pars:
    tol: 1.0e-6
    maxiter: 4000

metacal_pars:
    step: 0.01

ngal: 2000000

desired_hours: 0.5
# at bnl
#sec_per: 0.80
# at slac
sec_per: 0.50

verbose: True
