# playing around for now, may make more definite
run: "run-bd11mcal-t05"
sim: "sim-bd11"

fitter: "metacal-em"

deep_data:
  run: "run-bd11zmcal-degrade01"

# 1.0 gives mean s2n of ~12.3 with scatter 5 for the flux distribution in
# the sim
noise: 0.01

metacal_pars:
    step: 0.01

ngal: 2000000

desired_hours: 0.5
# at bnl
#sec_per: 0.80
# at slac
sec_per: 0.75

verbose: True
