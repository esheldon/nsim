run: "run-gg01r02"
sim: "sim-gg01"

# this is old, redo it
fitter: "crap"
fit_model: "gauss"

guess_type: "truth"

# starting number, may increase
n_samples: 50000

# effective number after weighting should be this many
min_eff_n_samples: 1000

expand_shear_true: true

# we normalize splits by split for is2n==0
desired_err: 4.5e-05
nsplit0: 20000

s2n_vals: [ 10 ]


