# same as sim-dg06 but higher shera
# for ngmix
#     gaussian psf
#     dev galaxy
#     FWHM ratio ~ 1.2

name: "sim-dg09"

psf_model: "gauss"
psf_T: 4.0
psf_shape: [0.0, 0.0]

obj_model: "dev"

cen_sigma: 0.1

g_prior_type: "ba"
g_prior_sigma: 0.3

obj_T_mean: 16.0
obj_T_sigma_frac: 0.3

obj_counts_mean: 100.0
obj_counts_sigma_frac: 0.3

shear: [0.035,0.035]

nsub: 16

label: '$dev fwhm/fwhm_{psf}=1.2$'
