# for ngmix
#     gaussian psf
#     exponential galaxy

name: "sim-eg08"

psf_model: "gauss"
psf_T: 4.0
psf_shape: [0.0, 0.0]

obj_model: "exp"

cen_sigma: 0.1

# joint size-flux distribution
joint_TF_dist: "cosmos-exp"
pixel_scale: 0.27

# s/n value at the flux mode
fmode_s2n: 100.0

# can be null
TF_bounds: [16.0, 0.0]

nsigma_render: 5.0

recenter_psf: true

shear: [0.08,0.0]

nsub: 16

label: '$exp FWHM/FWHM_{PSF}=1.2$'
